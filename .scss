toggleAllSelection(event: Event) {
  if (!this.focalEntityData?.focalEntities) return;

  const shouldSelect = !this.isAllSelected();

  this.selectedEntityIds.clear();

  if (shouldSelect) {
    this.focalEntityData.focalEntities.forEach(e => {
      this.selectedEntityIds.add(e.peId);
    });

    // select first entity for right panel
    this.selectedEntity = this.focalEntityData.focalEntities[0] || null;
  } else {
    this.selectedEntity = null;
  }

  this.selectedEntityChange.emit(this.selectedEntity);
}
